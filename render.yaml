services:
  - type: web
    name: telegram-text-bot
    env: static            # Rust binary için native runtime
    plan: starter          # Render’da geçerli plan
    buildCommand: cargo build --release
    startCommand: ./target/release/telegram_bot
    envVars:
      - key: BOT_TOKEN
        sync: false
    healthCheckPath: /
